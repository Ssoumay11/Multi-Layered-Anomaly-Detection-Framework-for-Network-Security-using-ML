# streamlit
# pandas
# numpy
# matplotlib
# seaborn
# scikit-learn
# tensorflow
# joblib
plotly


# AI-SOC Live Capture System
# Requirements & Setup Guide
# ============================================================

# ── Python Backend ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
pydantic==2.7.1

# Packet capture (REQUIRED)
scapy==2.5.0
psutil==5.9.8

# ML / data
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
tensorflow>=2.13          # or tensorflow-cpu

# Attack demo extras
colorama==0.4.6
requests==2.31.0

# ============================================================
# SETUP GUIDE
# ============================================================
#
# STEP 1 — Install Npcap (Windows) or confirm libpcap (Linux/Mac)
#   Windows: Download from https://npcap.com/#download
#            Install with "WinPcap API compatibility" checked
#   Linux:   sudo apt install libpcap-dev
#   Mac:     brew install libpcap
#
# STEP 2 — Install Python requirements
#   pip install -r requirements_live.txt
#
# STEP 3 — Copy your .pkl and .h5 model files to the same folder:
#   isolation_forest.pkl
#   packet_autoencoder.h5
#   behavioral_lstm.h5
#   packet_scaler.pkl
#   behavior_scaler.pkl
#   packet_thresholds.pkl
#   behavior_threshold.pkl
#
# STEP 4 — Start backend (MUST be run as Admin/root for raw socket):
#   Windows (Run as Administrator):
#     python live_capture_backend.py
#   Linux/Mac:
#     sudo python live_capture_backend.py
#
# STEP 5 — Open frontend:
#   Option A: Paste soc_live_dashboard.jsx into claude.ai artifact
#   Option B: Create React app:
#     npm create vite@latest soc-app -- --template react
#     cd soc-app && npm install recharts
#     # Replace src/App.jsx with soc_live_dashboard.jsx content
#     npm run dev
#     # Open http://localhost:5173
#
# STEP 6 — Run attack demos for judges:
#   python attack_demo.py
#   # Select from menu, watch dashboard react in real-time
#
# ============================================================
# DEMO TIPS FOR JUDGES
# ============================================================
#
# 1. SHOW NORMAL vs ATTACK CONTRAST:
#    - Run option 1 (Normal Traffic) — dashboard stays GREEN
#    - Immediately run option 2 (SYN Flood) — watch scores turn RED
#    - Show that attack_probability spikes from ~0.2 to ~0.9
#
# 2. SHOW 3-LAYER DIFFERENCE:
#    - Run option 6 (Botnet/Low-and-Slow)
#    - Switch to "3-LAYER PIPELINE" tab
#    - Show: Packet layer = LOW (misses it)
#            Flow layer   = LOW (misses it)
#            Behaviour    = HIGH (catches it!)
#    - This proves WHY multi-layer is essential
#
# 3. SHOW THE FUSION ENGINE:
#    - Click any RED event in the table
#    - Go to "3-LAYER PIPELINE" tab
#    - Walk through each layer's contribution
#    - Show majority vote decision
#
# 4. ALTERNATIVE ATTACK METHODS (without attack_demo.py):
#    a) nmap -sS 127.0.0.1        (port scan)
#    b) ping -n 1000 127.0.0.1   (ICMP flood — Windows)
#    c) ping -f -c 1000 127.0.0.1 (Linux)
#    d) Open 50 browser tabs to any website (normal traffic spike)
#    e) Run a large file download (wget, curl) for data burst pattern
#
# ============================================================
# API ENDPOINTS (for judges who want to see raw data)
# ============================================================
#
#   GET  http://localhost:8000/         — health check
#   GET  http://localhost:8000/health   — model status
#   GET  http://localhost:8000/events   — recent detections
#   GET  http://localhost:8000/stats    — summary statistics
#   GET  http://localhost:8000/flows    — live flow table
#   GET  http://localhost:8000/behaviour — per-IP behaviour
#   GET  http://localhost:8000/docs     — Swagger UI (auto-generated)
#   WS   ws://localhost:8000/ws         — real-time WebSocket stream